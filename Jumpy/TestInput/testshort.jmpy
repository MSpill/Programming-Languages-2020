int i = ( -1 + 7 ) * 5
int n = 0
|str{} myArr = str{2}|

myMarker:
    println i + 1
    i = i + 1
    jmp [i > 70 ? "end" : -6]

end:
    println "round done"
    n = n + 1
    i = 60
    jmp [n > 3 ? "realend" : "myMarker"]

realend:
    println "done"